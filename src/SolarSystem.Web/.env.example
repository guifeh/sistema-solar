# URL base da API, sem o sufixo /api (o client adiciona).
#   dotnet run  -> http://localhost:5268 (porta do launchSettings.json)
#   docker compose up -> http://localhost:5000
VITE_API_URL=http://localhost:5268
